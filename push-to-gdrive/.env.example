# Google Drive Configuration
# Copy this file to .env and fill in your values

# Optional: Default folder ID for uploads
# You can set this through the CLI with: python main.py set-folder
GOOGLE_DRIVE_DEFAULT_FOLDER_ID=

# Optional: Custom port for OAuth callback (default is 8080)
OAUTH_PORT=8080

# Optional: Custom config directory (default is current directory)
# GOOGLE_DRIVE_CONFIG_DIR=./config